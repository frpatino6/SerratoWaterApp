name: serrato_water_app
description: Credit application Serrato water.
publish_to: 'none'
version: 0.2.0+0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  http: ^1.3.0
  equatable: ^2.0.0
  flutter_signature_pad: ^3.0.1
  file_picker: ^8.1.7
  provider: ^6.0.5
  multi_select_flutter: ^4.1.3
  flutter_multiselect: ^1.0.0
  flutter_masked_text2: ^0.9.1
  keyboard_dismisser: ^3.0.0
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png    

